{
  "name": "whispernet",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "node scripts/env.config.js && next dev -p 4000",
    "build": "node scripts/env.config.js && next build",
    "start": "next start",
    "lint": "next lint"
  }
}
