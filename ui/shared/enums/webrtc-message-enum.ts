export enum WebRTCMessageEnum {
    Offer = 'offer',
    IncommingOffer = 'incomming_offer',
    Answer = 'answer',
    Candidate = 'candidate',
    Waiting = 'waiting',
    Found = 'found',
    Init = 'init',
    Join = 'join',
    Opened = 'opened'

}
